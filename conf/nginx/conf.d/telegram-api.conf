# Глобальні налаштування таймаутів
#fastcgi_read_timeout 300;
#fastcgi_connect_timeout 300;
#fastcgi_send_timeout 300;
#proxy_read_timeout 300;
#proxy_connect_timeout 300;
#proxy_send_timeout 300;

# Основний HTTP сервер (Traefik буде проксувати на цей порт)
server {
    listen 80;
    listen [::]:80;
    
    server_name destorage.jdymora.com;
    
    client_max_body_size 100M;
    client_body_timeout 300s;
    client_header_timeout 300s;
    
    root /var/www/telegram-api/src/public;
    
    index index.php index.html index.htm;
    error_log  /var/log/nginx/nginx_error.log;
    access_log /var/log/nginx/nginx_access.log;

    location / { 
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php-8:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        
        # Додаємо для правильної роботи з Traefik
        fastcgi_param HTTPS on;
        fastcgi_param HTTP_X_FORWARDED_PROTO https;
    }

    location ~ /\.ht {
        deny all;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}